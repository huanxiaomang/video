
windows

ipconfig 查看局域网ip

./mediamtx.exe

cd server
venv\Scripts\Activate.ps1
pip install -r requirements.txt
python -c "from models import init_db; import asyncio; asyncio.run(init_db())"
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

cd web-client
pnpm install
pnpm dev


linux

检查

cat /sys/class/thermal/cooling_device*/type

vcgencmd measure_temp

sudo raspi-config

列举连接的摄像头
libcamera-hello --list-cameras

测试摄像头
rpicam-hello
libcamera-still -o test.jpg --width 640 --height 480
libcamera-hello --width 640 --height 480 --framerate 15

修改配置
cd Desktop/video/camera-client
nano .env CTRL o保存 ctrl x退出 第一行改成服务端ip


启动mediamtx
ssh yuxiang@192.168.1.9  > 123456
cd ~/mediamtx
./mediamtx


启动采集端
ssh yuxiang@192.168.1.9  > 123456
cd Desktop/video/camera-client

source venv/bin/activate
pip install -r requirements.txt
python main.py
